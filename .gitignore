# IDEs
/.idea

# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

terraform-provider-scalr
terraform-provider-scalr_*
*.code-workspace
.DS_Store

# CI generated
gha-creds-*.json

covprofile
