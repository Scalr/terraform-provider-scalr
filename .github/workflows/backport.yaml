name: 'Backport Fixes'

on:
  pull_request:
    types:
      - opened
      - ready_for_review
      - reopened
      - synchronize
      - labeled
    branches:
      - 'release/*'

concurrency:
  group: ${{ github.ref }}-${{ github.workflow }}
  cancel-in-progress: true

jobs:
  backport:
    name: Backport
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
        filter: blob:none
        show-progress: false

    - name: Backport
      uses: Scalr/actions/2-pr-backport@SCALRCORE-33146
      with:
        approval_bot_token: ${{secrets.SCALR_BUILDBOT_TOKEN}}
        token: ${{github.token}}
        branches_ordering_rule: >-
          release/*.*.*
          master
        base_branch: ${{github.event.pull_request.base.ref}}
        head_branch: ${{github.event.pull_request.head.ref}}